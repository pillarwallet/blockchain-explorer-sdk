const sinon = require('sinon');
var assert = require("assert");
var sdk = require("../src/index.js");
var request = require("request-promise")

describe('Integration Tests', () => {
    it('Expected to get the correct balance object', () => {  

    });
});